# sql_finetune — Colab / Kaggle (T4 free tier)
# torch is pre-installed on both platforms; not pinned here to avoid CUDA conflicts.

# Data
datasets>=2.19.0
pandas>=2.0.0
pyarrow>=14.0.0
scikit-learn>=1.3.0
pyyaml>=6.0

# EDA
matplotlib>=3.7.0

# Model / training
transformers>=4.44.0
accelerate>=0.33.0
peft>=0.12.0
bitsandbytes>=0.43.0
trl<0.15.0

# Inference precision (used in 04-finetuned-eval-sql.ipynb)
torchao>=0.5.0
