# Core
pandas==2.2.2
numpy<2
PyYAML==6.0.1

# LangChain
langchain<1.0
langchain-core<1.0
langchain-text-splitters
langchain-community
langchain-chroma
langchain-huggingface
langchain-google-genai

# retriever.py imports these directly (device detection, progress bars) --
# usually pulled in transitively by sentence-transformers/langchain-huggingface,
# but declared explicitly since relying on someone else's transitive deps
# is how "works on my machine" happens.
torch
tqdm

# Retrieval (same models impl_vanilla uses, for a fair comparison)
sentence-transformers==2.7.0

# Eval
ragas==0.2.15

# Observability (optional -- see notebooks/02_rag_pipeline.ipynb's LangSmith cell)
langsmith

# Tracking / versioning
mlflow
dvc
dvc-gdrive==3.0.1
pathspec<0.12

# Dev
pytest==8.2.0
