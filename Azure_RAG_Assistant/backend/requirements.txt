fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.19
pydantic[email]>=2.11,<3.0
pydantic-settings>=2.7.0,<3.0
jinja2==3.1.5

# Agent framework (current, non-deprecated API: langchain.agents.create_agent)
langchain==1.3.14
langchain-text-splitters>=1.0.0,<2.0
langchain-google-genai==4.3.2
langchain-qdrant==1.1.0
langchain-huggingface==1.2.2
qdrant-client==1.19.0

# Self-hosted LLM backend option (LLM_BACKEND=local) — talks to Ollama's
# OpenAI-compatible endpoint (http://localhost:11434/v1)
langchain-openai>=1.0.0,<2.0

# Document processing
pdfplumber==0.11.5
pytesseract==0.3.13
Pillow==11.0.0

python-dotenv==1.0.1
azure-storage-blob==12.30.0

# Observability (ENABLE_OBSERVABILITY=true)
arize-phoenix-otel>=0.8.0
openinference-instrumentation-langchain>=0.1.40

# Auth + database
sqlalchemy>=2.0,<3.0
bcrypt>=4.0,<5.0
pyjwt>=2.9,<3.0
# SQL Server driver for production (Azure SQL). pymssql ships with its
# native dependencies bundled in the wheel, so no extra system packages
# or container setup needed - just this pip install. Used when
# DATABASE_URL points at mssql+pymssql://... (see database.py).
pymssql>=2.3,<3.0

# Tests + load testing
pytest==8.3.4
httpx==0.28.1
