# --- Data prep / EDA ---
datasets
pandas
matplotlib
seaborn
jupyter
ipykernel

# --- HF Spaces UI (this is what actually gets free ZeroGPU) ---
gradio

# --- Generic API backend (for a real-GPU deployment target, e.g. Azure) ---
fastapi
uvicorn[standard]
pydantic
requests

# --- Model inference (LoRA adapter loading + generation) ---
torch
unsloth
llama-cpp-python
transformers
accelerate
bitsandbytes
peft

# --- RAG retrieval ---
chromadb
sentence-transformers

# --- Evaluation (Gemini faithfulness check) ---
google-genai

# --- Hugging Face Spaces ZeroGPU wrapper (Gradio SDK only) ---
spaces

# --- HF Hub push (used from the training notebook) ---
huggingface_hub
