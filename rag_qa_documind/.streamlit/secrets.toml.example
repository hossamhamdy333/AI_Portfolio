# This is a template only 
# in  app's Settings -> Secrets in the dashboard instead (paste this same format there, with your real key).
GEMINI_API_KEY = "your-key-here"
GEMINI_MODEL = "gemini-3.1-flash-lite"

# Optional: require visitors to enter this code before using the app.
# Protects your free-tier Gemini quota from random public traffic.
# Leave commented out to keep the app open (e.g. for local dev).
# APP_PASSWORD = "choose-a-passcode"
