# ── Project 01 · Text Classification Pipeline ─────────────────────────────
# Pin every version. Never use floating deps in a reproducible ML project.
# Install: pip install -r requirements.txt
# Colab:   !pip install -r requirements.txt -q

# ── Data & Validation ──────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
pyarrow==16.0.0          # parquet read/write
pydantic==2.7.1          # schema validation
ydata-profiling==4.18.4   # automated EDA reports
scikit-learn==1.4.2

# ── Visualisation ──────────────────────────────────────────────────────────
matplotlib==3.8.4
seaborn==0.13.2

# ── Deep Learning ──────────────────────────────────────────────────────────
torch==2.3.0
transformers==4.41.1
datasets==2.19.1
accelerate==0.30.0
tokenizers==0.19.1

# ── ONNX Export & Serving ──────────────────────────────────────────────────
onnx==1.16.0
onnxruntime==1.18.0
optimum==1.19.2           # HuggingFace ONNX export helper

# ── Experiment Tracking ────────────────────────────────────────────────────
mlflow==2.12.2
wandb==0.17.0

# ── Data Version Control ───────────────────────────────────────────────────
dvc==3.50.1

# ── API Serving ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
httpx==0.27.0             # for testing endpoints

# ── Config & Utilities ─────────────────────────────────────────────────────
pyyaml==6.0.1
python-dotenv==1.0.1
tqdm==4.66.4
rich==13.7.1              # pretty terminal output

# ── Code Quality ───────────────────────────────────────────────────────────
pytest==8.2.0
ruff==0.4.4               # linting + formatting (replaces black + flake8)
